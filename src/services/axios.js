/** HTTP client placeholder — replace with axios/fetch when API is ready. */
export async function request(_config) {
  throw new Error('API client not configured. Use feature mock data for now.');
}
